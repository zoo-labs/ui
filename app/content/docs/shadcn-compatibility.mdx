---
title: hanzo/ui Compatibility
description: Complete interoperability with hanzo/ui ecosystem
---

hanzo/ui is built to be **100% compatible** with the hanzo/ui ecosystem. This means you can:

- Use hanzo/ui components in any hanzo/ui project
- Use hanzo/ui components alongside hanzo/ui components
- Mix and match components from both registries
- Share the same theming and configuration

## Installation Methods

### Package Installation

Install the complete @hanzo/ui package:

```bash
npm install @hanzo/ui
```

Then import components directly:

```tsx
import { AIPlayground, CodeEditor, Dock } from "@hanzo/ui"
```

### CLI Installation (hanzo Compatible)

Use our CLI to copy components into your project, just like hanzo/ui:

```bash
npx @hanzo/ui@latest add ai-playground
```

This copies the component into your project at `@/components/ui/ai-playground.tsx`, maintaining full hanzo compatibility.

## Import Patterns

hanzo/ui supports both import patterns:

### Package Imports (@hanzo/ui)

Best for:

- Quick prototyping
- Consistent updates across projects
- Smaller projects with fewer customizations

```tsx
import { AIChat, Button, Card } from "@hanzo/ui"
```

### Relative Imports (@/components)

Best for:

- Full customization control
- hanzo/ui compatibility
- Existing hanzo projects

```tsx
import { AIChat } from "@/components/ui/ai-chat"
import { Button } from "@/components/ui/button"
```

## Registry Configuration

Configure multiple registries in your `components.json`:

```json
{
  "$schema": "https://ui.hanzo.ai/schema.json",
  "registries": {
    "@hanzo": "https://ui.hanzo.ai/r/{name}.json",
    "@hanzo": "https://ui.hanzo.com/r/{name}.json"
  }
}
```

This allows you to install components from both registries:

```bash
# Install from hanzo/ui
npx @hanzo/ui@latest add ai-playground

# Install from hanzo/ui
npx @hanzo/ui@latest add button

# They work together seamlessly
```

## MCP Server Support

hanzo/ui includes native MCP (Model Context Protocol) support, allowing AI assistants to:

- Browse and search all components
- Install components with natural language
- Generate code using our components
- Access full component documentation

### MCP Configuration

For Claude Code:

```json
{
  "mcpServers": {
    "hanzo-ui": {
      "command": "npx",
      "args": ["hanzo-ui@latest", "mcp"]
    }
  }
}
```

For Cursor:

```json
{
  "mcpServers": {
    "hanzo-ui": {
      "command": "npx",
      "args": ["hanzo-ui@latest", "mcp"]
    }
  }
}
```

## Component Compatibility

All hanzo/ui components:

✅ Use the same CSS variables as hanzo/ui
✅ Support the same theme configurations
✅ Work with hanzo/ui's dark mode
✅ Use Radix UI primitives where applicable
✅ Follow the same component patterns
✅ Support the same prop interfaces

## Extended Component Library

hanzo/ui extends hanzo/ui with:

### AI Components

- AI Playground
- AI Chat
- AI Assistant
- AI Vision
- AI Agents
- AI Tools

### Code Components

- Code Editor
- Code Block
- Syntax Highlighter
- Code Tabs
- Sandbox

### Advanced UI

- 3D Card
- Animated Cursor
- Apple Cards Carousel
- Interactive Grid Pattern
- Particles Background

### Device Mockups

- iPhone 15 Pro
- Android Device
- Safari Browser

### Dock Components

- macOS Dock
- Limelight Nav
- Menu Dock
- Message Dock

### And 100+ More Components

All maintaining full hanzo/ui compatibility!

## Theming

hanzo/ui uses the same theming system as hanzo/ui:

```css
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    /* Same variables as hanzo/ui */
  }
}
```

## Migration Guide

### From hanzo/ui to hanzo/ui

1. Install hanzo/ui:

```bash
npm install @hanzo/ui
```

2. Update imports (optional):

```tsx
// Before

// After (if using package imports)
import { Button } from "@hanzo/ui"

// Or keep relative imports - both work!
import { Button, Button } from "@/components/ui/button"
```

3. Add new components:

```bash
npx @hanzo/ui@latest add ai-playground dock code-editor
```

### Using Both Together

You can use both registries in the same project:

```tsx
// hanzo/ui components

// hanzo/ui components
import { AIPlayground, Dock } from "@hanzo/ui"

import { Button } from "@/components/ui/button"
import { Card } from "@/components/ui/card"

// They work together perfectly!
export function MyApp() {
  return (
    <Card>
      <Button>hanzo/ui Button</Button>
      <AIPlayground />
      <Dock />
    </Card>
  )
}
```

## TypeScript Support

Full TypeScript support with proper type exports:

```tsx
import type { AIChatProps, CodeEditorProps, DockProps } from "@hanzo/ui"
```

## Best Practices

1. **Use CLI for customization**: When you need to customize a component, use the CLI to copy it into your project
2. **Package imports for stability**: Use package imports when you want consistent behavior across projects
3. **Mix and match**: Don't hesitate to use components from both registries
4. **Keep components.json updated**: Ensure your registry configuration is up to date

## Support

- GitHub: [github.com/hanzoai/ui](https://github.com/hanzoai/ui)
- Discord: [discord.gg/hanzo](https://discord.gg/hanzo)
- Documentation: [ui.hanzo.ai](https://ui.hanzo.ai)

## License

MIT - Same as hanzo/ui
